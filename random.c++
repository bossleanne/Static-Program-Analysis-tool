#include <vector>
#include <string>

vector<string> s;
while (token.at(i) != ')'){
	s.push_back[token.at(i)] //condition inside
}


while (token.at(i) != '}'){
	s.push_back[token.at(i)] //content inside
}
